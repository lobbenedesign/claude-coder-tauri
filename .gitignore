# Rust Build Artifacts
/src-tauri/target/
target/
**/*.rs.bk
Cargo.lock

# Dependencies
node_modules/
bun.lockb
# package-lock.json is intentionally NOT ignored: it's needed for
# `npm ci` in .github/workflows/ci.yml to get a reproducible frontend
# test install (see ui/*.test.js, run via vitest).

# Local Environment & Secrets
.env
.env.local
*.log

# OS Metadata
.DS_Store
Thumbs.db
